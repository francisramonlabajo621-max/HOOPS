# PythonAnywhere Quick Commands for HOOPS

# Step 1: Navigate to home and clone repository
cd ~
git clone https://github.com/francisramonlabajo621-max/HOOPS.git HOOPS

# Step 2: Create virtual environment
python3.11 -m venv ~/.virtualenvs/hoops

# Step 3: Activate virtual environment
source ~/.virtualenvs/hoops/bin/activate

# Step 4: Upgrade pip
pip install --upgrade pip

# Step 5: Install dependencies
pip install -r ~/HOOPS/requirements.txt

# Step 6: Initialize database (run in Python console)
cd ~/HOOPS
python3.11
# Then in Python:
# from app import app, db
# with app.app_context():
#     db.create_all()
# exit()

# WSGI File Content (/var/www/<username>_pythonanywhere_com_wsgi.py):
# import sys
# from pathlib import Path
# 
# project_root = Path('/home/<your-username>/HOOPS')
# if str(project_root) not in sys.path:
#     sys.path.insert(0, str(project_root))
# 
# from app import app as application

# Web App Configuration:
# Working directory: /home/<your-username>/HOOPS
# Virtualenv: /home/<your-username>/.virtualenvs/hoops
# Environment Variable: SECRET_KEY = <your-secret-key>

